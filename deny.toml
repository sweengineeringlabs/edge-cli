# cargo-deny configuration for CLI workspace
# Checks for dependency vulnerabilities and licensing issues

[advisories]
# The path where the advisory database is cloned/fetched into
db-path = "$CARGO_HOME/advisory-db"
# How to handle crates with security vulnerabilities
vulnerability = "deny"
# How to handle unmaintained crates
unmaintained = "warn"
# How to handle crates that have been yanked from crates.io
yanked = "warn"
# How to handle crates with security notices
notice = "warn"

[licenses]
# How to handle crates which do not have a detectable license
unlicensed = "deny"
# List of explicitly allowed licenses
allow = [
  "Apache-2.0",
  "MIT",
  "Apache-2.0 OR MIT",
]

[bans]
# Lint level for when multiple dependency versions are included
multiple-versions = "warn"
